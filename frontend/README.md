# frontend

React (Vite) reader for the Loony Library API. See [../docs/dev_setup.md](../docs/dev_setup.md) for how this fits with the backend and database.

```sh
npm install
cp .env.example .env   # VITE_API_URL, defaults to http://localhost:4000
npm run dev             # http://localhost:5173
```
